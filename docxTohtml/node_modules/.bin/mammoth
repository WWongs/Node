#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_mammoth@1.4.9@mammoth/bin/mammoth" "$@"
  ret=$?
else 
  node  "$basedir/../_mammoth@1.4.9@mammoth/bin/mammoth" "$@"
  ret=$?
fi
exit $ret
